# 国际论文线（物尽其用 / Gleaner Hybrid）实施计划 #13

> **For agentic workers:** REQUIRED SUB-SKILL: 用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实施。步骤用 checkbox（`- [ ]`）跟踪。

**Goal:** 给 gleaner 采集层加"国际论文"线——主题→OpenAlex 发现→分层取全文（OA→Sci-Hub→订阅库 CARSI）→统一落 `corpus/` 喂 0131，首要覆盖五大经济学顶刊（学校未订阅）。

**Architecture:** 发现与取全文分离。**发现**走 OpenAlex（公开 API，主力机本地）。**取全文**按论文来源路由：①OA 论文 → Unpaywall/OpenAlex/Crossref 直链；②闭源/五大顶刊 → Sci-Hub/LibGen 按 DOI（主力机本地，绕 Clash 直连）；③学校订阅库（ScienceDirect/EBSCO/Emerald/Springer/ABI）→ CARSI browser-SSO 登一次复用 cookie。**不自动化 SAML 联邦登录**。全部新源不复用 CNKI 的 `scraper/downloader/captcha`（强耦合），但复用 `browser.py`/cookie/`run_batch` 入口约定/`acq_mcp` 派发骨架。

**Tech Stack:** Python 3.11（备用机 conda env `acq` / 主力机 `C:\Users\laidh\anaconda3`）、`urllib`+`requests`、Playwright（订阅库分支）、FastMCP（gleaner）、pytest。蓝本：Apache-2.0 `Rimagination/scansci-pdf` 开源部分（已抓到 scratchpad，仅借鉴源码不跑其闭源 `_core`）。

## Global Constraints（每个任务都隐含包含本节，数值/规则照抄）

1. **直连铁律（最高优先）**：所有外网 API/下载一律绕过 Clash 全局代理。`urllib` 用 `build_opener(ProxyHandler({}), HTTPSHandler(context=ctx))`；`requests` 用 `Session(); sess.trust_env=False`。**否则 OpenAlex 间歇失败、Unpaywall/Sci-Hub 几乎必挂 `SSL: UNEXPECTED_EOF_WHILE_READING`。** 例外：访问国内 IdP（`*.carsi.edu.cn`、浙商大 CAS）时也要直连/DIRECT，不能走海外节点。
2. **PDF 三层校验（落盘必过）**：①`is_plausible_pdf_url`（URL 形态预筛，可关）②`_response_looks_pdf`（首 chunk `startswith(b"%PDF-")` 或 `content-type` 含 `application/pdf`）③`is_pdf_file`（`size>=1000`、头 5 字节 `==b"%PDF-"`、末 1024 字节含 `b"%%EOF"`）。下载统一 `.part`→`os.replace()` 原子改名。
3. **RateGuard 强制（当前未实现，必须新建）**：每次订阅库/Sci-Hub 下载前过闸。订阅库**并发=1**、单库随机延迟 10–30s、每日配额小、命中 403/429/被踢 WAYF 立即熔断该源。OA API 调用间 `uniform(2,5)s` 礼貌延迟。配额按**北京日期**跨天清零、持久化。
4. **DOI 规范化 + 跨源去重**：入库前 `normalize_doi_unicode`（修全角连字符/空格）；中文 CNKI 与英文国际按 DOI 去重。
5. **统一 metadata schema**：国际批次 CSV 列含 `source, doi, oa_status, pdf_url` + 现有 `run_batch.FIELDS`。`utf-8-sig`，原子写，按 DOI（无 DOI 退 title）去重合并。
6. **MCP 工具无返回类型注解**：`@mcp.tool()` 函数**不写** `-> str`/`-> dict`（避免 FastMCP 自动 outputSchema 包装与预格式化 JSON 文本冲突）。
7. **批参数走 UTF-8 JSON 文件**（不走命令行，规避 SSH 中文/引号乱码），照 `run_batch.py` 约定。
8. **Sci-Hub 法律灰色**：合法 OA（Unpaywall/OpenAlex/Crossref/DOAJ/arXiv）为主力且优先；`scihub_enabled` 设为可配开关（laidh 已选启用，默认 True），保留礼貌限速。
9. **CARSI cookie 在出版商域**：登录用 `context.storage_state()` 存 cookies+localStorage（多域），下载用 `new_context(storage_state=...)` 灌回；**不要**用 `downloader.py` 那个拍平 name→value 的 requests 写法做跨域下载。

---

## 文件结构

新建一个 `acq/` 包收口国际线（与现有扁平 CNKI 文件并存，不动 CNKI）：

| 文件 | 职责 |
|---|---|
| `acq/__init__.py` | 包标记（空） |
| `acq/net.py` | 直连 opener/session 工厂（绕 Clash）+ 统一 UA/超时 |
| `acq/pdfcheck.py` | PDF 三层校验 + 原子落盘 |
| `acq/identifiers.py` | DOI/arXiv 规范化 |
| `acq/guard.py` | RateGuard：per-source 限速 + 每日配额（持久化/北京日）+ 熔断 |
| `acq/sources/__init__.py` | 子包标记 |
| `acq/sources/openalex.py` | 发现层：关键词/概念/ISSN/年份 → DOI+元数据 |
| `acq/sources/oa.py` | OA 取全文：Unpaywall→OpenAlex→Crossref→DOAJ |
| `acq/sources/scihub.py` | Sci-Hub/LibGen 按 DOI 取闭源 PDF |
| `acq/sources/carsi.py` | 订阅库 headless 下载（复用存好的 storage_state） |
| `acq/cookie_store.py` | 多域 cookie/storage_state 持久化 + 失效探测 |
| `acq/data/publisher_carsi.json` | 出版商配置（蓝本底 + 补 EBSCO/Emerald/ABI） |
| `acq/intl_downloader.py` | 单 DOI 取全文编排：OA→订阅→Sci-Hub，全过 guard |
| `acq/config_intl.py` | 国际线常量：镜像表、endpoint、mailto、配额默认、出版商入口 |
| `carsi_login.py` | 顶层有头一次性登录脚本（人工过 CAS + 遍历点库存 cookie） |
| `run_intl_batch.py` | 国际批入口（读 JSON）：发现→去重→下载→统一 CSV |
| `acq_mcp.py`（改） | 加 `intl_collect` 工具 + `_run_collect` 泛化 `script` 参数 + `list_sources` 扩条 |
| `tests/` | 各任务 pytest |

> 注：`tests/` 用 pytest。涉及外网的测试用 `@pytest.mark.network` 标注，可 `-m "not network"` 跳过；纯逻辑（校验/规范化/guard/schema）全部离线可测。

---

## Phase 0：公共底座

### Task 1：PDF 三层校验 + 原子落盘（`acq/pdfcheck.py`）

**Files:**
- Create: `acq/pdfcheck.py`
- Test: `tests/test_pdfcheck.py`

**Interfaces:**
- Produces:
  - `is_plausible_pdf_url(url: str) -> bool`
  - `response_looks_pdf(first_chunk: bytes, content_type: str) -> bool`
  - `is_pdf_file(path: str | Path, min_size: int = 1000) -> bool`
  - `write_pdf_atomic(resp_iter, out_path: Path) -> bool`（流式写 `.part`→校验→`os.replace`，失败清理返回 False）

- [ ] **Step 1: 写失败测试**

```python
# tests/test_pdfcheck.py
from pathlib import Path
from acq.pdfcheck import is_plausible_pdf_url, response_looks_pdf, is_pdf_file

def test_plausible_pdf_url():
    assert is_plausible_pdf_url("https://x.com/a.pdf")
    assert is_plausible_pdf_url("https://x.com/science/article/pii/123/pdfft")
    assert is_plausible_pdf_url("https://x.com/d?format=pdf")
    assert not is_plausible_pdf_url("https://x.com/journals/list")
    assert not is_plausible_pdf_url("ftp://x.com/a.pdf")

def test_response_looks_pdf():
    assert response_looks_pdf(b"%PDF-1.5 ...", "text/html")
    assert response_looks_pdf(b"<html>", "application/pdf; charset=utf-8")
    assert not response_looks_pdf(b"<html>", "text/html")

def test_is_pdf_file(tmp_path):
    good = tmp_path / "g.pdf"
    good.write_bytes(b"%PDF-1.4" + b"x" * 2000 + b"%%EOF")
    assert is_pdf_file(good)
    small = tmp_path / "s.pdf"; small.write_bytes(b"%PDF-")
    assert not is_pdf_file(small)
    nohdr = tmp_path / "n.pdf"; nohdr.write_bytes(b"<html>" + b"x" * 2000 + b"%%EOF")
    assert not is_pdf_file(nohdr)
    noeof = tmp_path / "e.pdf"; noeof.write_bytes(b"%PDF-" + b"x" * 2000)
    assert not is_pdf_file(noeof)
```

- [ ] **Step 2: 跑测试确认失败** — `pytest tests/test_pdfcheck.py -v` → FAIL（模块不存在）

- [ ] **Step 3: 实现**

```python
# acq/pdfcheck.py
import os
import re
from pathlib import Path
from urllib.parse import urlparse

_BAD_PATH = ("/data-providers/", "/providers/", "/journals/", "/subjects/")

def is_plausible_pdf_url(url: str) -> bool:
    if not url or not url.lower().startswith(("http://", "https://")):
        return False
    p = urlparse(url)
    path = p.path.lower()
    if any(b in path for b in _BAD_PATH):
        return False
    q = p.query.lower()
    return (path.endswith(".pdf") or "/pdf" in path or "download/pdf" in path
            or "format=pdf" in q or "type=pdf" in q
            or path.endswith("/document"))  # HAL 特例

def response_looks_pdf(first_chunk: bytes, content_type: str) -> bool:
    if first_chunk[:5] == b"%PDF-":
        return True
    return "application/pdf" in (content_type or "").lower()

def is_pdf_file(path, min_size: int = 1000) -> bool:
    path = Path(path)
    try:
        size = path.stat().st_size
    except OSError:
        return False
    if size < min_size:
        return False
    with open(path, "rb") as f:
        if f.read(5) != b"%PDF-":
            return False
        f.seek(max(0, size - 1024))
        return b"%%EOF" in f.read()

def write_pdf_atomic(resp_iter, out_path) -> bool:
    """resp_iter: 可迭代 bytes 块。写 .part→校验→原子改名。"""
    out_path = Path(out_path)
    part = out_path.with_suffix(out_path.suffix + ".part")
    try:
        with open(part, "wb") as f:
            for chunk in resp_iter:
                if chunk:
                    f.write(chunk)
        if not is_pdf_file(part):
            part.unlink(missing_ok=True)
            return False
        os.replace(part, out_path)
        return True
    except Exception:
        part.unlink(missing_ok=True)
        return False
```

- [ ] **Step 4: 跑测试确认通过** — `pytest tests/test_pdfcheck.py -v` → PASS

- [ ] **Step 5: 提交** — `git add acq/pdfcheck.py tests/test_pdfcheck.py && git commit -m "feat(intl): PDF 三层校验+原子落盘"`

---

### Task 2：DOI/arXiv 规范化（`acq/identifiers.py`）

**Files:** Create `acq/identifiers.py`；Test `tests/test_identifiers.py`

**Interfaces — Produces:**
- `normalize_doi(v: str) -> str`（去 `https://doi.org/`/`doi:` 前缀 + urldecode）
- `normalize_doi_unicode(v: str) -> str | None`（修全角连字符/空格，校验 `^10.\d{4,}/`，非法返回 None）
- `safe_filename(s: str) -> str`
- `arxiv_pdf_url(arxiv_id: str) -> str`

- [ ] **Step 1: 写失败测试**

```python
# tests/test_identifiers.py
from acq.identifiers import normalize_doi, normalize_doi_unicode, safe_filename

def test_normalize_doi():
    assert normalize_doi("https://doi.org/10.1257/aer.91.5.1369") == "10.1257/aer.91.5.1369"
    assert normalize_doi("doi: 10.1093/qje/qjac001") == "10.1093/qje/qjac001"

def test_normalize_doi_unicode():
    # 全角连字符 U+2010 + 多余空格（PDF 复制常见）
    assert normalize_doi_unicode("10.1257/aer.91.5.1369") == "10.1257/aer.91.5.1369"
    assert normalize_doi_unicode("10.1257/aer‐5 1369") is not None
    assert normalize_doi_unicode("not-a-doi") is None

def test_safe_filename():
    assert safe_filename('a/b:c*?"d') == "a_b_c_d"
    assert safe_filename("") == "paper"
```

- [ ] **Step 2: 跑测试确认失败**

- [ ] **Step 3: 实现**（照蓝本 `identifiers.py`）

```python
# acq/identifiers.py
import re
from urllib.parse import unquote

def normalize_doi(v: str) -> str:
    v = re.sub(r"^https?://(?:dx\.)?doi\.org/", "", v or "", flags=re.I)
    v = re.sub(r"^doi:\s*", "", v, flags=re.I)
    return unquote(v).strip()

def normalize_doi_unicode(v: str):
    if not v:
        return None
    v = normalize_doi(v)
    for ch in ("‐", "‑", "‒", "–", "—"):
        v = v.replace(ch, "-")
    v = re.sub(r"\s+", "", v)
    return v if re.match(r"^10\.\d{4,}/", v) else None

def safe_filename(s: str) -> str:
    s = re.sub(r"[^A-Za-z0-9._\-一-鿿]+", "_", s or "").strip("_")
    return s[:100] or "paper"

def arxiv_pdf_url(arxiv_id: str) -> str:
    return f"https://arxiv.org/pdf/{arxiv_id}.pdf"
```

- [ ] **Step 4: 跑测试确认通过**
- [ ] **Step 5: 提交** — `git commit -m "feat(intl): DOI/arXiv 规范化"`

---

### Task 3：直连网络底座（`acq/net.py`）

**Files:** Create `acq/net.py`；Test `tests/test_net.py`

**Interfaces — Produces:**
- `direct_opener() -> urllib.request.OpenerDirector`（`ProxyHandler({})` + TLS context）
- `get(url, headers=None, timeout=25) -> (status:int, headers:dict, body:bytes)`（直连 GET，整体读）
- `get_stream(url, headers=None, timeout=25)`（返回 response 供流式 `iter`，调用方负责关闭）
- `direct_requests_session()`（`requests.Session` + `trust_env=False`，给订阅/Sci-Hub 用）
- 常量 `USER_AGENT`

- [ ] **Step 1: 写失败测试**（离线测：opener 无代理）

```python
# tests/test_net.py
from acq import net

def test_direct_opener_has_no_proxy():
    op = net.direct_opener()
    # ProxyHandler({}) 注入后，handlers 里 ProxyHandler.proxies 为空
    proxies = [h.proxies for h in op.handlers if h.__class__.__name__ == "ProxyHandler"]
    assert proxies and all(p == {} for p in proxies)

def test_user_agent_present():
    assert "Mozilla/5.0" in net.USER_AGENT
```

- [ ] **Step 2: 跑测试确认失败**

- [ ] **Step 3: 实现**（照实测金事实：`ProxyHandler({})` 直连）

```python
# acq/net.py
import ssl
import urllib.request

USER_AGENT = ("Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 "
              "(KHTML, like Gecko) Chrome/124.0 Safari/537.36")

def _ctx(insecure: bool = False) -> ssl.SSLContext:
    ctx = ssl.create_default_context()
    if insecure:  # Sci-Hub CDN 偶发异常证书时用
        ctx.check_hostname = False
        ctx.verify_mode = ssl.CERT_NONE
    return ctx

def direct_opener(insecure: bool = False):
    # 关键：ProxyHandler({}) 绕过 Clash，否则 SSL UNEXPECTED_EOF
    return urllib.request.build_opener(
        urllib.request.ProxyHandler({}),
        urllib.request.HTTPSHandler(context=_ctx(insecure)),
    )

def _req(url, headers=None):
    h = {"User-Agent": USER_AGENT}
    if headers:
        h.update(headers)
    return urllib.request.Request(url, headers=h)

def get(url, headers=None, timeout=25, insecure=False):
    op = direct_opener(insecure)
    with op.open(_req(url, headers), timeout=timeout) as r:
        return r.status, dict(r.headers), r.read()

def get_stream(url, headers=None, timeout=25, insecure=False):
    op = direct_opener(insecure)
    return op.open(_req(url, headers), timeout=timeout)  # 调用方 with/close

def direct_requests_session():
    import requests
    s = requests.Session()
    s.trust_env = False          # 绕过环境代理（Clash）
    s.headers.update({"User-Agent": USER_AGENT})
    return s
```

- [ ] **Step 4: 跑测试确认通过**
- [ ] **Step 5:（可选联网自测）** `python -c "from acq import net; print(net.get('https://api.openalex.org/works?per-page=1&mailto=Libby_Stantoncsc@writeme.com')[0])"` → 期望 `200`
- [ ] **Step 6: 提交** — `git commit -m "feat(intl): 直连网络底座(绕Clash)"`

---

### Task 4：RateGuard 安全闸（`acq/guard.py`）

**Files:** Create `acq/guard.py`；Test `tests/test_guard.py`

**Interfaces — Produces:** `class RateGuard`
- `__init__(self, source: str, min_interval: float, max_per_day: int, state_dir: Path)`
- `can_download(self) -> bool`（未熔断且当日配额未满）
- `wait(self)`（距上次 ≥ `min_interval`，含 ±20% 抖动）
- `record(self, ok: bool)`（落盘当日计数 +1；连续失败累计）
- `trip(self, reason: str)` / `tripped(self) -> bool`（熔断/查询）
- `remaining(self) -> int`
- 配额按**北京日期**（`UTC+8`）键名跨天清零，持久化 `state_dir/guard_<source>.json`

- [ ] **Step 1: 写失败测试**（注入 `now` 可控时间，避免 sleep）

```python
# tests/test_guard.py
from pathlib import Path
from acq.guard import RateGuard

def test_quota_and_circuit(tmp_path):
    g = RateGuard("test", min_interval=0, max_per_day=2, state_dir=tmp_path)
    assert g.can_download()
    g.record(ok=True); g.record(ok=True)
    assert not g.can_download()          # 配额用完
    assert g.remaining() == 0

def test_trip(tmp_path):
    g = RateGuard("test", min_interval=0, max_per_day=99, state_dir=tmp_path)
    assert not g.tripped()
    g.trip("403")
    assert g.tripped() and not g.can_download()

def test_persist_same_day(tmp_path):
    g1 = RateGuard("sd", min_interval=0, max_per_day=5, state_dir=tmp_path)
    g1.record(ok=True)
    g2 = RateGuard("sd", min_interval=0, max_per_day=5, state_dir=tmp_path)
    assert g2.remaining() == 4           # 跨实例同日持久化
```

- [ ] **Step 2: 跑测试确认失败**

- [ ] **Step 3: 实现**

```python
# acq/guard.py
import json
import random
import time
from datetime import datetime, timezone, timedelta
from pathlib import Path

_CST = timezone(timedelta(hours=8))  # 北京时区

def _today() -> str:
    return datetime.now(_CST).strftime("%Y-%m-%d")

class RateGuard:
    def __init__(self, source, min_interval, max_per_day, state_dir):
        self.source = source
        self.min_interval = float(min_interval)
        self.max_per_day = int(max_per_day)
        self.state_dir = Path(state_dir)
        self.state_dir.mkdir(parents=True, exist_ok=True)
        self._path = self.state_dir / f"guard_{source}.json"
        self._last = 0.0
        self._state = self._load()

    def _load(self):
        if self._path.exists():
            try:
                s = json.loads(self._path.read_text("utf-8"))
            except Exception:
                s = {}
        else:
            s = {}
        if s.get("day") != _today():           # 跨天清零
            s = {"day": _today(), "count": 0, "tripped": False, "reason": ""}
            self._save(s)
        return s

    def _save(self, s=None):
        (s or self._state) and self._path.write_text(
            json.dumps(s or self._state, ensure_ascii=False), "utf-8")

    def remaining(self) -> int:
        if self._state.get("day") != _today():
            self._state = self._load()
        return max(0, self.max_per_day - self._state.get("count", 0))

    def tripped(self) -> bool:
        return bool(self._state.get("tripped"))

    def can_download(self) -> bool:
        return (not self.tripped()) and self.remaining() > 0

    def wait(self):
        if self.min_interval <= 0:
            return
        delta = time.monotonic() - self._last
        jitter = self.min_interval * random.uniform(0.8, 1.2)
        if delta < jitter:
            time.sleep(jitter - delta)
        self._last = time.monotonic()

    def record(self, ok: bool):
        self._state["count"] = self._state.get("count", 0) + 1
        self._save()

    def trip(self, reason: str):
        self._state["tripped"] = True
        self._state["reason"] = reason
        self._save()
```

- [ ] **Step 4: 跑测试确认通过**
- [ ] **Step 5: 提交** — `git commit -m "feat(intl): RateGuard 限速/每日配额/熔断"`

---

## Phase 1：OA + Sci-Hub 取全文（主力机，无需校园 IP）— 覆盖五大顶刊

### Task 5：OpenAlex 发现层（`acq/sources/openalex.py`）

**Files:** Create `acq/sources/openalex.py`, `acq/sources/__init__.py`；Test `tests/test_openalex.py`

**Interfaces — Produces:**
- `search(query: str, num: int = 25, *, econ_only=True, year_from: str | None = None, issn: str | None = None, mailto: str) -> list[dict]`
- 返回每条 dict：`{doi, title, year, journal, is_oa, oa_status, oa_url, authors, institutions}`
- 内部 `_iter_works(params, num, mailto)` 用 cursor 深翻；`per-page` 封顶 200。

**实测要点（来自侦查）：** 无需 key；`mailto` 进 polite pool；`filter=` 比 `search=` 便宜一个数量级；经济学概念 `concepts.id:C162324750`；`per-page<=200`；cursor 首传 `*`，读 `meta.next_cursor`；顶刊 `pdf_url` 常为 null（发现层只负责给 DOI，PDF 留给取全文层）。

- [ ] **Step 1: 写失败测试**（解析逻辑离线测，喂样例 JSON）

```python
# tests/test_openalex.py
from acq.sources.openalex import _parse_work

SAMPLE = {
    "doi": "https://doi.org/10.1093/qje/qjag031",
    "title": "What Jobs Come to Mind?",
    "publication_year": 2026,
    "open_access": {"is_oa": True, "oa_status": "hybrid", "oa_url": "https://doi.org/10.1093/qje/qjag031"},
    "primary_location": {"source": {"display_name": "The Quarterly Journal of Economics",
                                    "issn": ["0033-5533"]}, "pdf_url": None},
    "authorships": [{"author": {"display_name": "John J. Conlon"},
                     "institutions": [{"display_name": "Carnegie Mellon University"}]}],
}

def test_parse_work():
    w = _parse_work(SAMPLE)
    assert w["doi"] == "10.1093/qje/qjag031"      # 去 doi.org 前缀
    assert w["journal"].startswith("The Quarterly")
    assert w["is_oa"] is True and w["oa_status"] == "hybrid"
    assert w["authors"] == ["John J. Conlon"]
```

- [ ] **Step 2: 跑测试确认失败**

- [ ] **Step 3: 实现**

```python
# acq/sources/openalex.py
import json
from urllib.parse import urlencode, quote
from acq import net
from acq.identifiers import normalize_doi

BASE = "https://api.openalex.org/works"
ECON_CONCEPT = "C162324750"

def _parse_work(w: dict) -> dict:
    oa = w.get("open_access") or {}
    src = (w.get("primary_location") or {}).get("source") or {}
    auths = w.get("authorships") or []
    return {
        "doi": normalize_doi(w.get("doi") or "") or "",
        "title": w.get("title") or "",
        "year": w.get("publication_year"),
        "journal": src.get("display_name") or "",
        "is_oa": bool(oa.get("is_oa")),
        "oa_status": oa.get("oa_status") or "",
        "oa_url": oa.get("oa_url") or (w.get("primary_location") or {}).get("pdf_url") or "",
        "authors": [a.get("author", {}).get("display_name", "") for a in auths],
        "institutions": [i.get("display_name", "")
                         for a in auths for i in (a.get("institutions") or [])],
    }

def _iter_works(filters: str, search: str, num: int, mailto: str):
    got, cursor = 0, "*"
    while got < num and cursor:
        params = {"per-page": min(200, num - got), "cursor": cursor, "mailto": mailto}
        if filters:
            params["filter"] = filters
        if search:
            params["search"] = search
        status, _, body = net.get(f"{BASE}?{urlencode(params)}")
        if status != 200:
            break
        data = json.loads(body)
        results = data.get("results", [])
        if not results:
            break
        for w in results:
            yield w
            got += 1
            if got >= num:
                return
        cursor = (data.get("meta") or {}).get("next_cursor")

def search(query, num=25, *, econ_only=True, year_from=None, issn=None,
           mailto="Libby_Stantoncsc@writeme.com") -> list:
    fil = []
    if econ_only:
        fil.append(f"concepts.id:{ECON_CONCEPT}")
    if year_from:
        fil.append(f"from_publication_date:{year_from}")
    if issn:
        fil.append(f"primary_location.source.issn:{issn}")
    fil.append("has_doi:true")
    return [_parse_work(w) for w in _iter_works(",".join(fil), query, num, mailto)]
```

- [ ] **Step 4: 跑测试确认通过**
- [ ] **Step 5:（联网自测，可选）** `python -c "from acq.sources.openalex import search; r=search('minimum wage employment', 3); print(len(r), r[0]['doi'])"` → 期望 3 条带 DOI
- [ ] **Step 6: 提交** — `git commit -m "feat(intl): OpenAlex 发现层"`

---

### Task 6：OA 取全文（`acq/sources/oa.py`）

**Files:** Create `acq/sources/oa.py`；Test `tests/test_oa.py`

**Interfaces — Produces:**
- `resolve_oa_pdf_urls(doi: str, *, email: str) -> list[str]`（按 Unpaywall→OpenAlex→Crossref→DOAJ 顺序收集候选 PDF 直链，repo 优先于 publisher）
- `download_oa(doi: str, out_path: Path, *, email: str) -> bool`（逐候选下载，过 PDF 三层校验）

**实测要点：** Unpaywall `email` 强制（不带→422）；闭源顶刊 Unpaywall `best_oa_location=None`（返回空候选属正常）；OpenAlex `open_access.oa_url`；Crossref `message.link[]` 里 `content-type==application/pdf`。

- [ ] **Step 1: 写失败测试**（解析候选逻辑离线测）

```python
# tests/test_oa.py
from acq.sources.oa import _unpaywall_candidates, _crossref_candidates

UNPAYWALL = {"best_oa_location": {"host_type": "publisher", "url_for_pdf": "https://p/pub.pdf"},
             "oa_locations": [{"host_type": "repository", "url_for_pdf": "https://r/repo.pdf"},
                              {"host_type": "publisher", "url_for_pdf": "https://p/pub.pdf"}]}

def test_unpaywall_repo_first():
    c = _unpaywall_candidates(UNPAYWALL)
    assert c[0] == "https://r/repo.pdf"   # repo 优先于 publisher
    assert "https://p/pub.pdf" in c

def test_unpaywall_empty_closed():
    assert _unpaywall_candidates({"best_oa_location": None, "oa_locations": []}) == []

def test_crossref_pdf_link():
    cr = {"message": {"link": [{"content-type": "application/pdf", "URL": "https://x/a.pdf"},
                               {"content-type": "text/html", "URL": "https://x/h"}]}}
    assert _crossref_candidates(cr) == ["https://x/a.pdf"]
```

- [ ] **Step 2: 跑测试确认失败**

- [ ] **Step 3: 实现**

```python
# acq/sources/oa.py
import json
from urllib.parse import quote
from pathlib import Path
from acq import net
from acq.pdfcheck import is_plausible_pdf_url, response_looks_pdf, write_pdf_atomic

def _dedup(seq):
    out, seen = [], set()
    for x in seq:
        if x and x not in seen:
            seen.add(x); out.append(x)
    return out

def _unpaywall_candidates(data: dict) -> list:
    repo, pub = [], []
    best = data.get("best_oa_location") or {}
    locs = data.get("oa_locations") or []
    for loc in ([best] if best else []) + locs:
        u = loc.get("url_for_pdf")
        if not u:
            continue
        (repo if loc.get("host_type") == "repository" else pub).append(u)
    return _dedup(repo + pub)

def _crossref_candidates(data: dict) -> list:
    msg = data.get("message") or {}
    return _dedup(l.get("URL") for l in (msg.get("link") or [])
                  if (l.get("content-type") or "").lower() == "application/pdf")

def resolve_oa_pdf_urls(doi: str, *, email: str) -> list:
    cands = []
    try:  # Unpaywall
        s, _, b = net.get(f"https://api.unpaywall.org/v2/{quote(doi)}?email={email}")
        if s == 200:
            cands += _unpaywall_candidates(json.loads(b))
    except Exception:
        pass
    try:  # OpenAlex
        s, _, b = net.get(f"https://api.openalex.org/works/doi:{quote(doi)}?mailto={email}")
        if s == 200:
            d = json.loads(b); oa = d.get("open_access") or {}
            if oa.get("is_oa") and oa.get("oa_url"):
                cands.append(oa["oa_url"])
    except Exception:
        pass
    try:  # Crossref
        s, _, b = net.get(f"https://api.crossref.org/works/{quote(doi)}")
        if s == 200:
            cands += _crossref_candidates(json.loads(b))
    except Exception:
        pass
    return [u for u in _dedup(cands) if is_plausible_pdf_url(u)]

def download_oa(doi: str, out_path, *, email: str) -> bool:
    for url in resolve_oa_pdf_urls(doi, email=email):
        try:
            r = net.get_stream(url)
            first = r.read(8192)
            if not response_looks_pdf(first, r.headers.get("Content-Type", "")):
                r.close(); continue
            def _it():
                yield first
                while True:
                    c = r.read(65536)
                    if not c:
                        break
                    yield c
            ok = write_pdf_atomic(_it(), out_path)
            r.close()
            if ok:
                return True
        except Exception:
            continue
    return False
```

- [ ] **Step 4: 跑测试确认通过**
- [ ] **Step 5: 提交** — `git commit -m "feat(intl): OA 取全文(Unpaywall/OpenAlex/Crossref)"`

---

### Task 7：Sci-Hub 取闭源全文（`acq/sources/scihub.py`）

**Files:** Create `acq/sources/scihub.py`；Test `tests/test_scihub.py`

**Interfaces — Produces:**
- `extract_pdf_url_from_html(html: str, base_url: str) -> str | None`
- `download_scihub(doi: str, out_path: Path, *, mirrors: list[str] | None = None) -> bool`

**实测金事实（务必照做）：** 唯一稳定活镜像 `sci-hub.usualwant.com`，PDF 真链走 CDN `https://sci.bban.top/pdf/{DOI}.pdf`；**必须绕 Clash 直连**（`net` 已封）；落地页可能是 Cloudflare Turnstile 验证页（无浏览器只能换镜像）；真实有效 DOI 命中率高、伪 DOI 易撞验证墙；取直链带落地页 cookies、`require_pdf_like_url=False`。

- [ ] **Step 1: 写失败测试**（HTML 抽取离线测）

```python
# tests/test_scihub.py
from acq.sources.scihub import extract_pdf_url_from_html

def test_extract_citation_meta():
    html = '<meta name="citation_pdf_url" content="https://sci.bban.top/pdf/10.1/x.pdf">'
    assert extract_pdf_url_from_html(html, "https://sci-hub.x") == "https://sci.bban.top/pdf/10.1/x.pdf"

def test_extract_iframe():
    html = '<iframe src="//sci.bban.top/pdf/10.1/y.pdf#nav"></iframe>'
    got = extract_pdf_url_from_html(html, "https://sci-hub.usualwant.com")
    assert got.endswith("/pdf/10.1/y.pdf") and got.startswith("https://")

def test_extract_none():
    assert extract_pdf_url_from_html("<embed src=''>", "https://x") is None
```

- [ ] **Step 2: 跑测试确认失败**

- [ ] **Step 3: 实现**

```python
# acq/sources/scihub.py
import re
from urllib.parse import quote, urljoin
from pathlib import Path
from acq import net
from acq.pdfcheck import is_plausible_pdf_url, response_looks_pdf, write_pdf_atomic

DEFAULT_MIRRORS = [
    "https://sci-hub.usualwant.com",  # 实测唯一稳定出 PDF（2026-06-29）
    "https://sci-hub.st", "https://sci-hub.ru", "https://sci-hub.se",
]
BBAN_CDN = "https://sci.bban.top/pdf/{doi}.pdf"
_NOT_FOUND = ("article not found", "статья не найдена", "не найден")
_META_RE = re.compile(r'citation_pdf_url"[^>]*content="([^"]+)"', re.I)
_META_RE2 = re.compile(r'content="([^"]+)"[^>]*name="citation_pdf_url"', re.I)
_SRC_RE = re.compile(r'(?:src|href)=["\']([^"\']+)["\']', re.I)

def extract_pdf_url_from_html(html: str, base_url: str):
    for rx in (_META_RE, _META_RE2):
        m = rx.search(html)
        if m:
            return urljoin(base_url, m.group(1))
    for m in _SRC_RE.finditer(html):
        u = urljoin(base_url, m.group(1))
        if is_plausible_pdf_url(u):
            return u
    return None

def _dl(url, out_path, cookies=None) -> bool:
    try:
        headers = {}
        r = net.get_stream(url, headers=headers, insecure=True)
        first = r.read(8192)
        if not response_looks_pdf(first, r.headers.get("Content-Type", "")):
            r.close(); return False
        def _it():
            yield first
            while True:
                c = r.read(65536)
                if not c:
                    break
                yield c
        ok = write_pdf_atomic(_it(), out_path)
        r.close()
        return ok
    except Exception:
        return False

def download_scihub(doi: str, out_path, *, mirrors=None) -> bool:
    # 先试 CDN 直链（实测最快命中）
    if _dl(BBAN_CDN.format(doi=doi), out_path):
        return True
    for domain in (mirrors or DEFAULT_MIRRORS):
        url = f"{domain.rstrip('/')}/{quote(doi, safe='/')}"
        try:
            r = net.get_stream(url, insecure=True)
            first = r.read(8192)
            ctype = r.headers.get("Content-Type", "")
            if response_looks_pdf(first, ctype):       # 落地页直接是 PDF
                def _it():
                    yield first
                    while True:
                        c = r.read(65536)
                        if not c:
                            break
                        yield c
                ok = write_pdf_atomic(_it(), out_path); r.close()
                if ok:
                    return True
                continue
            body = (first + r.read(512_000)).decode("utf-8", "ignore")
            r.close()
            low = body[:5000].lower()
            if "captcha" in low or "turnstile" in low:
                continue                                # 撞验证墙，换镜像
            if any(s in body.lower() for s in _NOT_FOUND):
                continue
            pdf_url = extract_pdf_url_from_html(body, url)
            if pdf_url and _dl(pdf_url, out_path):
                return True
        except Exception:
            continue
    return False
```

- [ ] **Step 4: 跑测试确认通过** — `pytest tests/test_scihub.py -v`
- [ ] **Step 5:（联网自测，强烈建议）** 用实测可下的真实 DOI：
  `python -c "from acq.sources.scihub import download_scihub; from pathlib import Path; print(download_scihub('10.1257/aer.91.5.1369', Path('aer.pdf')))"` → 期望 `True` 且 `aer.pdf` 是 ~190KB 真 PDF
- [ ] **Step 6: 提交** — `git commit -m "feat(intl): Sci-Hub 取闭源全文(usualwant+bban CDN)"`

---

### Task 8：单 DOI 取全文编排（`acq/intl_downloader.py`）

**Files:** Create `acq/intl_downloader.py`；Test `tests/test_intl_downloader.py`

**Interfaces:**
- Consumes: `oa.download_oa`、`scihub.download_scihub`、`guard.RateGuard`、`pdfcheck`
- Produces: `download(meta: dict, out_dir: Path, guards: dict[str, RateGuard], *, email: str, scihub_enabled=True) -> tuple[str, str]`
  - 返回 `(file_path, source)`，失败 `("", "")`，对齐 CNKI `downloader.download_paper` 契约
  - 路由：`meta["is_oa"]` 或先试 OA；失败且 `scihub_enabled` → Sci-Hub。每次过对应 guard（`wait`/`can_download`/`record`/`trip`）

- [ ] **Step 1: 写失败测试**（用 monkeypatch 桩掉网络）

```python
# tests/test_intl_downloader.py
from pathlib import Path
from acq import intl_downloader as dl
from acq.guard import RateGuard

def test_oa_first(tmp_path, monkeypatch):
    monkeypatch.setattr(dl.oa, "download_oa", lambda doi, p, **k: Path(p).write_bytes(b"%PDF-"+b"x"*2000+b"%%EOF") or True)
    monkeypatch.setattr(dl.scihub, "download_scihub", lambda *a, **k: (_ for _ in ()).throw(AssertionError("不该走 Sci-Hub")))
    guards = {"oa": RateGuard("oa", 0, 99, tmp_path), "scihub": RateGuard("scihub", 0, 99, tmp_path)}
    path, src = dl.download({"doi": "10.1/x", "title": "T", "is_oa": True}, tmp_path, guards, email="e@x.com")
    assert src == "oa" and path.endswith(".pdf")

def test_scihub_fallback(tmp_path, monkeypatch):
    monkeypatch.setattr(dl.oa, "download_oa", lambda *a, **k: False)
    monkeypatch.setattr(dl.scihub, "download_scihub", lambda doi, p, **k: Path(p).write_bytes(b"%PDF-"+b"x"*2000+b"%%EOF") or True)
    guards = {"oa": RateGuard("oa", 0, 99, tmp_path), "scihub": RateGuard("scihub", 0, 99, tmp_path)}
    path, src = dl.download({"doi": "10.1/y", "title": "T2", "is_oa": False}, tmp_path, guards, email="e@x.com")
    assert src == "scihub"

def test_guard_blocks_scihub(tmp_path, monkeypatch):
    monkeypatch.setattr(dl.oa, "download_oa", lambda *a, **k: False)
    g = RateGuard("scihub", 0, 0, tmp_path)  # 配额=0
    guards = {"oa": RateGuard("oa", 0, 99, tmp_path), "scihub": g}
    path, src = dl.download({"doi": "10.1/z", "title": "T3", "is_oa": False}, tmp_path, guards, email="e@x.com")
    assert (path, src) == ("", "")
```

- [ ] **Step 2: 跑测试确认失败**

- [ ] **Step 3: 实现**

```python
# acq/intl_downloader.py
from pathlib import Path
from acq.sources import oa, scihub
from acq.identifiers import safe_filename

def download(meta, out_dir, guards, *, email, scihub_enabled=True):
    doi = meta.get("doi") or ""
    if not doi:
        return ("", "")
    out_dir = Path(out_dir)
    out_dir.mkdir(parents=True, exist_ok=True)
    out_path = out_dir / f"{safe_filename(meta.get('title') or doi)}.pdf"
    if out_path.exists() and out_path.stat().st_size > 0:
        return (str(out_path), "cache")

    # 1) OA 优先（合法、免费）
    g = guards.get("oa")
    if not g or g.can_download():
        g and g.wait()
        try:
            if oa.download_oa(doi, out_path, email=email):
                g and g.record(ok=True)
                return (str(out_path), "oa")
        except Exception:
            pass
        g and g.record(ok=False)

    # 2) Sci-Hub 兜底（闭源顶刊；灰色，受 guard 管）
    if scihub_enabled:
        g = guards.get("scihub")
        if not g or g.can_download():
            g and g.wait()
            try:
                if scihub.download_scihub(doi, out_path):
                    g and g.record(ok=True)
                    return (str(out_path), "scihub")
            except Exception:
                pass
            g and g.record(ok=False)
    return ("", "")
```

- [ ] **Step 4: 跑测试确认通过**
- [ ] **Step 5: 提交** — `git commit -m "feat(intl): 单DOI取全文编排(OA→SciHub+guard)"`

---

## Phase 2：订阅库 CARSI browser-SSO（登一次复用 cookie）

> **关键事实（侦查纠正）**：授权 cookie 在出版商域（`_shibsession_*`），不在 CARSI 域。一次 CAS 登录 + 把目标库各点一遍 → `storage_state` 一把存全部域。下载用 `new_context(storage_state=...)`。**登录用自带 chromium（不挂 msedge channel）避免继承系统 Clash 代理**；国内 IdP（`*.carsi.edu.cn`/浙商大 CAS）须直连/DIRECT。

### Task 9：多域 cookie/storage_state 存储（`acq/cookie_store.py`）

**Files:** Create `acq/cookie_store.py`；Test `tests/test_cookie_store.py`

**Interfaces — Produces:** `class CookieStore`
- `__init__(self, base_dir: Path)`（目录式，区别于 CNKI 单文件 `cookies.json`）
- `state_path(self) -> Path`（`base_dir/state.json`，整 storage_state）
- `save_state(self, storage_state: dict)` / `load_state(self) -> dict | None`
- `age_hours(self) -> float | None`（state.json 修改时间距今小时数；蓝本 24h 阈值当**硬上限**，真判活靠下载探测）

- [ ] **Step 1: 写失败测试**

```python
# tests/test_cookie_store.py
from acq.cookie_store import CookieStore

def test_save_load_state(tmp_path):
    cs = CookieStore(tmp_path)
    assert cs.load_state() is None
    cs.save_state({"cookies": [{"name": "a", "value": "1", "domain": ".sciencedirect.com"}], "origins": []})
    s = cs.load_state()
    assert s["cookies"][0]["domain"] == ".sciencedirect.com"
    assert cs.age_hours() is not None and cs.age_hours() < 1
```

- [ ] **Step 2: 跑测试确认失败**
- [ ] **Step 3: 实现**

```python
# acq/cookie_store.py
import json
import time
from pathlib import Path

class CookieStore:
    def __init__(self, base_dir):
        self.base_dir = Path(base_dir)
        self.base_dir.mkdir(parents=True, exist_ok=True)

    def state_path(self):
        return self.base_dir / "state.json"

    def save_state(self, storage_state: dict):
        self.state_path().write_text(json.dumps(storage_state, ensure_ascii=False), "utf-8")

    def load_state(self):
        p = self.state_path()
        if not p.exists():
            return None
        try:
            return json.loads(p.read_text("utf-8"))
        except Exception:
            return None

    def age_hours(self):
        p = self.state_path()
        if not p.exists():
            return None
        return (time.time() - p.stat().st_mtime) / 3600.0
```

- [ ] **Step 4: 跑测试确认通过**
- [ ] **Step 5: 提交** — `git commit -m "feat(intl): 多域 storage_state cookie 存储"`

---

### Task 10：出版商配置数据（`acq/data/publisher_carsi.json`）

**Files:** Create `acq/data/publisher_carsi.json`；Test `tests/test_publisher_config.py`

**说明：** 蓝本那份覆盖 sciencedirect/springer/wiley/ieee/tandf/nature/asce/sage，**缺 EBSCO/Emerald/ABI**。本任务建一份精简配置，含本校 CARSI 订阅的核心库。`pdf_pattern` 能拼直链的填，拼不了的（ProQuest/EBSCO 会话绑定动态链）留空、靠 response 监听抓。

- [ ] **Step 1: 写失败测试**

```python
# tests/test_publisher_config.py
import json
from pathlib import Path

def test_config_shape():
    cfg = json.loads(Path("acq/data/publisher_carsi.json").read_text("utf-8"))
    for key in ("sciencedirect", "springer", "ebsco", "emerald", "proquest"):
        assert key in cfg, f"缺 {key}"
        assert "domains" in cfg[key] and isinstance(cfg[key]["domains"], list)
```

- [ ] **Step 2: 跑测试确认失败**
- [ ] **Step 3: 实现**

```json
{
  "sciencedirect": {
    "domains": ["sciencedirect.com", "elsevier.com"],
    "pdf_pattern": "https://www.sciencedirect.com/science/article/pii/{pii}/pdfft",
    "needs_response_capture": false
  },
  "springer": {
    "domains": ["link.springer.com", "springer.com"],
    "pdf_pattern": "https://link.springer.com/content/pdf/{doi}.pdf",
    "needs_response_capture": false
  },
  "emerald": {
    "domains": ["emerald.com"],
    "pdf_pattern": "https://www.emerald.com/insight/content/doi/{doi}/full/pdf",
    "needs_response_capture": false
  },
  "ebsco": {
    "domains": ["ebscohost.com", "ebsco.com"],
    "pdf_pattern": "",
    "needs_response_capture": true
  },
  "proquest": {
    "domains": ["proquest.com"],
    "pdf_pattern": "",
    "needs_response_capture": true
  }
}
```

- [ ] **Step 4: 跑测试确认通过**
- [ ] **Step 5: 提交** — `git commit -m "feat(intl): 出版商 CARSI 配置(补 EBSCO/Emerald/ABI)"`

---

### Task 11：有头一次性登录脚本（`carsi_login.py`）

**Files:** Create `carsi_login.py`（顶层，仿 `login.py`）

**说明：** 人工脚本，无单元测试，靠手动验收。复用 `browser.launch_browser()`（headed，**不挂 msedge channel** 以避开系统 Clash 代理），壳子抄 `login.py:19-74` 的"自动存+关窗即停"。

**Interfaces:** CLI：`python carsi_login.py`，登录后把 `context.storage_state()` 存到 `acq/cookies/carsi/state.json`。

- [ ] **Step 1: 实现**

```python
# carsi_login.py —— 有头人工登录一次：过 CAS + 把目标库各点一遍 → 存 storage_state
import os
import time
from pathlib import Path
os.environ["ACQ_HEADLESS"] = "0"
os.environ["ACQ_BROWSER_CHANNEL"] = ""   # 用自带 chromium，避开系统 Clash 代理
from browser import launch_browser       # 复用现有浏览器层
from acq.cookie_store import CookieStore

CARSI_PORTAL = "https://ds.carsi.edu.cn/ds/index.html"
TARGETS = ["ScienceDirect", "Springer", "EBSCO", "Emerald", "ProQuest"]
STORE = CookieStore(Path(__file__).parent / "acq" / "cookies" / "carsi")

def main():
    pw, browser, context, page = launch_browser()  # 见 browser.launch_browser 返回签名（实施时核对）
    try:
        page.goto(CARSI_PORTAL)
        print("=" * 60)
        print("请在浏览器里：1) 选『浙江工商大学』 2) 完成统一身份认证(CAS)")
        print(f"   3) 把这些库各点开一次(种 cookie)：{', '.join(TARGETS)}")
        print("   每库进到已认证主页即可，无需下载。完成后回车保存。")
        print("=" * 60)
        # 每 5s 自动存一次 storage_state，最长 10 分钟，关窗即停
        deadline = time.time() + 600
        while time.time() < deadline:
            try:
                STORE.save_state(context.storage_state())
            except Exception:
                break
            time.sleep(5)
        STORE.save_state(context.storage_state())
        print(f"已保存 storage_state → {STORE.state_path()}")
    finally:
        try:
            context.close(); browser.close(); pw.stop()
        except Exception:
            pass

if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 手动验收** — 在能交互的机器跑（主力机即可，CARSI 认人不认 IP），登录+点库后确认 `acq/cookies/carsi/state.json` 生成且含多个出版商域 cookie（`grep sciencedirect`/`ebsco`）。
- [ ] **Step 3: 提交** — `git commit -m "feat(intl): CARSI 有头一次性登录脚本"`

> **实施注意**：`browser.launch_browser()` 的真实返回签名要在 Task 实施时核对（侦查报告称 `browser.py:76`，但未给出确切返回元组）。若它不返回 `(pw,browser,context,page)`，按其实际签名调整本脚本。

---

### Task 12：订阅库 headless 下载（`acq/sources/carsi.py`）

**Files:** Create `acq/sources/carsi.py`；Test `tests/test_carsi.py`（仅离线可测部分）

**Interfaces — Produces:**
- `detect_publisher(url_or_doi: str, cfg: dict) -> str | None`（按域名/DOI 前缀匹配出版商 key）
- `session_alive(state: dict, probe_url: str) -> bool`（URL 回踢探测：落到含 `wayf/shibboleth/saml/idp/cas/login` 即死）
- `download_subscription(doi: str, out_path: Path, *, store: CookieStore, cfg: dict, guard: RateGuard) -> bool`（Playwright headless + storage_state，response 监听抓 PDF + pdf_pattern 兜底 + 点击兜底）

- [ ] **Step 1: 写失败测试**（detect/alive 离线测）

```python
# tests/test_carsi.py
import json
from pathlib import Path
from acq.sources.carsi import detect_publisher, session_alive

CFG = json.loads(Path("acq/data/publisher_carsi.json").read_text("utf-8"))

def test_detect_publisher():
    assert detect_publisher("https://www.sciencedirect.com/science/article/pii/X", CFG) == "sciencedirect"
    assert detect_publisher("https://www.emerald.com/insight/content/doi/10.1/full", CFG) == "emerald"
    assert detect_publisher("https://unknown.com/x", CFG) is None

def test_session_alive():
    assert session_alive({}, "https://www.sciencedirect.com/article/x") is True  # 未回踢
    assert session_alive({}, "https://idp.zjsu.edu.cn/cas/login?...") is False     # 回踢登录页
```

- [ ] **Step 2: 跑测试确认失败**

- [ ] **Step 3: 实现**（核心骨架；选择器/`pdfft` 拼链在实施时按真实页面核对）

```python
# acq/sources/carsi.py
import re
from urllib.parse import urlparse
from pathlib import Path

_KICK = re.compile(r"(wayf|shibboleth|saml|/idp|/cas|/login)", re.I)

def detect_publisher(url_or_doi: str, cfg: dict):
    host = urlparse(url_or_doi).netloc.lower()
    for key, c in cfg.items():
        if any(d in host for d in c.get("domains", [])):
            return key
    return None

def session_alive(state: dict, probe_url: str) -> bool:
    # 简化离线判据：probe_url 自身像登录/wayf 页即视为死会话
    return not bool(_KICK.search(probe_url or ""))

def download_subscription(doi, out_path, *, store, cfg, guard) -> bool:
    from playwright.sync_api import sync_playwright
    from acq.pdfcheck import is_pdf_file
    state = store.load_state()
    if not state:
        return False
    if not guard.can_download():
        return False
    guard.wait()
    out_path = Path(out_path)
    landing = f"https://doi.org/{doi}"
    got = {"ok": False}
    with sync_playwright() as pw:
        browser = pw.chromium.launch(headless=True)
        ctx = browser.new_context(storage_state=state, accept_downloads=True)
        page = ctx.new_page()

        def on_resp(resp):
            try:
                ct = (resp.headers or {}).get("content-type", "")
                if "application/pdf" in ct.lower() and not got["ok"]:
                    body = resp.body()
                    if body[:5] == b"%PDF-":
                        out_path.write_bytes(body)
                        got["ok"] = is_pdf_file(out_path)
            except Exception:
                pass
        page.on("response", on_resp)
        try:
            page.goto(landing, wait_until="domcontentloaded", timeout=45000)
            # 会话回踢检测
            if not session_alive(state, page.url):
                guard.record(ok=False)
                ctx.close(); browser.close()
                return False
            page.wait_for_timeout(3000)
            # 兜底1：pdf_pattern 直链
            if not got["ok"]:
                key = detect_publisher(page.url, cfg) or detect_publisher(landing, cfg)
                pat = (cfg.get(key) or {}).get("pdf_pattern") if key else ""
                if pat and "{doi}" in pat:
                    page.goto(pat.format(doi=doi), wait_until="load", timeout=45000)
                    page.wait_for_timeout(2000)
            # 兜底2：页面里点 a[href*=pdf]
            if not got["ok"]:
                link = page.query_selector("a[href*='pdf'], a[href*='pdfft']")
                if link:
                    with ctx.expect_page() as np:  # 可能开新标签
                        link.click()
                    page.wait_for_timeout(3000)
        except Exception:
            pass
        finally:
            guard.record(ok=got["ok"])
            ctx.close(); browser.close()
    return got["ok"]
```

- [ ] **Step 4: 跑离线测试确认通过** — `pytest tests/test_carsi.py -v`
- [ ] **Step 5:（手动联网验收）** 先跑过 Task 11 登录，再用一个**学校订阅库里**的真实 DOI（如某 ScienceDirect 经管论文）测 `download_subscription` 能落真 PDF。
- [ ] **Step 6: 提交** — `git commit -m "feat(intl): 订阅库 CARSI headless 下载"`

---

## Phase 3：批编排 + MCP 集成 + 跨源去重

### Task 13：国际批入口（`run_intl_batch.py`）

**Files:** Create `run_intl_batch.py`；Test `tests/test_run_intl_batch.py`（编排/去重/CSV 离线测）

**Interfaces:**
- Consumes: `openalex.search`、`intl_downloader.download`、`carsi.download_subscription`、`guard.RateGuard`
- 读 UTF-8 JSON 参数：`{query, num, out, year_from?, issn?, sources?: ["oa","scihub","carsi"], email?}`
- 产物：`out/<batch>/metadata.csv`（统一 schema）+ `out/<batch>/papers/*.pdf`
- Produces: `unified_fields() -> list[str]`、`dedup_by_doi(rows) -> list`、`save_csv(rows, out_dir)`、`main()`
- **统一 CSV 列**：`source, doi, oa_status, title, authors, journal, year, pdf_url, url, file_path, file_type`

- [ ] **Step 1: 写失败测试**

```python
# tests/test_run_intl_batch.py
from pathlib import Path
import run_intl_batch as r

def test_dedup_by_doi():
    rows = [{"doi": "10.1/a", "title": "A"}, {"doi": "10.1/A", "title": "A dup"}, {"doi": "", "title": "X"}]
    out = r.dedup_by_doi(rows)
    assert len(out) == 2   # 10.1/a 与 10.1/A 视同一(小写)，空 DOI 保留

def test_save_csv_unified(tmp_path):
    rows = [{"source": "oa", "doi": "10.1/a", "title": "T", "file_type": "PDF"}]
    r.save_csv(rows, tmp_path)
    txt = (tmp_path / "metadata.csv").read_text("utf-8-sig")
    assert "source" in txt and "oa_status" in txt and "10.1/a" in txt
```

- [ ] **Step 2: 跑测试确认失败**

- [ ] **Step 3: 实现**（编排：发现→去重→逐条下载，OA/SciHub 走 `intl_downloader`、订阅走 `carsi`；全过 guard；统一落 CSV。复用 `run_batch.save_csv` 原子写模式但用统一 FIELDS。`scihub_enabled` 由 `sources` 含 `"scihub"` 决定。）

```python
# run_intl_batch.py  （关键骨架，完整实现照本结构）
import csv, json, sys
from pathlib import Path
from acq.guard import RateGuard
from acq.cookie_store import CookieStore
from acq.identifiers import normalize_doi_unicode
from acq.sources import openalex, carsi as carsi_src
from acq import intl_downloader

FIELDS = ["source", "doi", "oa_status", "title", "authors", "journal",
          "year", "pdf_url", "url", "file_path", "file_type"]
STATE_DIR = Path(__file__).parent / "acq" / "guard_state"

def unified_fields():
    return list(FIELDS)

def dedup_by_doi(rows):
    out, seen = [], set()
    for row in rows:
        d = (row.get("doi") or "").lower()
        if d and d in seen:
            continue
        if d:
            seen.add(d)
        out.append(row)
    return out

def save_csv(rows, out_dir):
    out_dir = Path(out_dir); out_dir.mkdir(parents=True, exist_ok=True)
    path = out_dir / "metadata.csv"
    tmp = out_dir / "metadata.csv.tmp"
    with open(tmp, "w", encoding="utf-8-sig", newline="") as f:
        w = csv.DictWriter(f, fieldnames=FIELDS, extrasaction="ignore")
        w.writeheader()
        for row in rows:
            w.writerow(row)
    tmp.replace(path)

def main():
    pf = sys.argv[1] if len(sys.argv) > 1 else "intl_params.json"
    p = json.loads(Path(pf).read_text(encoding="utf-8"))
    query, num = p.get("query", ""), int(p.get("num", 25))
    out_dir = Path(p.get("out", "out_intl")); papers = out_dir / "papers"
    sources = p.get("sources", ["oa", "scihub"])
    email = p.get("email", "Libby_Stantoncsc@writeme.com")
    scihub_on = "scihub" in sources

    guards = {
        "oa": RateGuard("oa", min_interval=2, max_per_day=10000, state_dir=STATE_DIR),
        "scihub": RateGuard("scihub", min_interval=10, max_per_day=100, state_dir=STATE_DIR),
        "carsi": RateGuard("carsi", min_interval=15, max_per_day=50, state_dir=STATE_DIR),
    }
    # 注意：openalex.search 的 mailto 为强制参数(无默认)，必须显式传，否则 raise
    metas = dedup_by_doi(openalex.search(query, num, year_from=p.get("year_from"),
                                         issn=p.get("issn"), mailto=email))
    rows = []
    cfg = json.loads((Path(__file__).parent / "acq" / "data" / "publisher_carsi.json").read_text("utf-8"))
    store = CookieStore(Path(__file__).parent / "acq" / "cookies" / "carsi")
    for m in metas:
        m["doi"] = normalize_doi_unicode(m.get("doi") or "") or m.get("doi", "")
        fp, src = intl_downloader.download(m, papers, guards, email=email, scihub_enabled=scihub_on)
        if not fp and "carsi" in sources and m.get("doi"):
            if carsi_src.download_subscription(
                    m["doi"], papers / f"{m['doi'].replace('/', '_')}.pdf",
                    store=store, cfg=cfg, guard=guards["carsi"]):
                fp, src = str(papers / f"{m['doi'].replace('/', '_')}.pdf"), "carsi"
        rows.append({"source": src or "", "doi": m.get("doi", ""), "oa_status": m.get("oa_status", ""),
                     "title": m.get("title", ""), "authors": "; ".join(m.get("authors", [])),
                     "journal": m.get("journal", ""), "year": m.get("year", ""),
                     "pdf_url": m.get("oa_url", ""), "url": f"https://doi.org/{m.get('doi','')}",
                     "file_path": fp, "file_type": "PDF" if fp else ""})
    save_csv(rows, out_dir)
    print(json.dumps({"query": query, "found": len(metas),
                      "downloaded": sum(1 for r in rows if r["file_path"])}, ensure_ascii=False))

if __name__ == "__main__":
    main()
```

- [ ] **Step 4: 跑测试确认通过**
- [ ] **Step 5:（联网自测）** 写 `intl_params.json`（`{"query":"minimum wage employment","num":5,"out":"out_intl","sources":["oa","scihub"]}`）→ `python run_intl_batch.py intl_params.json` → 期望下到几篇真 PDF + `metadata.csv`
- [ ] **Step 6: 提交** — `git commit -m "feat(intl): 国际批入口(发现→去重→分层下载→统一CSV)"`

---

### Task 14：MCP 集成（`acq_mcp.py` 扩展）

**Files:** Modify `acq_mcp.py`；Test `tests/test_acq_mcp_intl.py`（`_run_collect` 泛化逻辑离线测）

**Interfaces:**
- 改 `_run_collect(query, num=20, pro=False, out_name="", script="run_batch.py")`：加 `script` 参数，派发对应脚本（CNKI=`run_batch.py`，国际=`run_intl_batch.py`）。参数 JSON 按 script 分别组装。
- 新增工具 `intl_collect(query, num=25, sources=None, year_from="", issn="", out_name="")`（**无返回类型注解**）：本地或经 SSH 跑 `run_intl_batch.py`。**OA/SciHub 分支可在主力机本地直跑**（无需校园 IP），订阅分支才需 cookie（也在主力机，CARSI 认人不认 IP）——故国际线**默认本地跑**，不走 SSH。
- 改 `list_sources()`：加 `openalex/oa/scihub/carsi` 条目 + 各自 guard 配额状态。

- [ ] **Step 1: 写失败测试**（验证 `_run_collect` 接受 `script` 参数、`intl_collect` 已注册）

```python
# tests/test_acq_mcp_intl.py
import inspect
import acq_mcp

def test_run_collect_has_script_param():
    assert "script" in inspect.signature(acq_mcp._run_collect).parameters

def test_intl_collect_registered():
    assert hasattr(acq_mcp, "intl_collect")

def test_intl_collect_no_return_annotation():
    assert inspect.signature(acq_mcp.intl_collect).return_annotation is inspect.Signature.empty
```

- [ ] **Step 2: 跑测试确认失败**
- [ ] **Step 3: 实现**（在现有 `acq_mcp.py` 上加：`intl_collect` 工具本地跑 `run_intl_batch.py`、把产物从 `out_intl/<batch>` 复制进 `LOCAL_CORPUS`；`list_sources` 扩条；`_run_collect` 加 `script` 参数）。**严格保持工具函数无返回类型注解。**
- [ ] **Step 4: 跑测试确认通过**
- [ ] **Step 5:（集成自测）** 重启 CC 后 `intl_collect("digital economy firm innovation", 5)` → corpus 里出现国际批次。
- [ ] **Step 6: 提交** — `git commit -m "feat(intl): gleaner 加 intl_collect 工具 + list_sources 扩条"`

---

### Task 15：跨源去重 + 喂 0131 收口

**Files:** Modify `run_intl_batch.py`（已有 `dedup_by_doi`）；新增 `tests/test_cross_source_dedup.py`

**说明：** 国际批与 CNKI 批最终都进 `corpus/`。本任务确保 0131 消费侧能按 DOI 跨中英文去重。统一 schema 已含 `source/doi`。CNKI 批的 `metadata.csv` 缺 `source/oa_status` 列——加一个轻量归一器 `acq/normalize_corpus.py`：把任意批次 CSV 读进来、补齐统一列、按 DOI（无 DOI 退 title）合并去重，输出 `corpus/merged_metadata.csv`。

**Interfaces — Produces:** `merge_corpus(corpus_dir: Path) -> Path`（扫所有 `*/metadata.csv`→补列→去重→写 `merged_metadata.csv`）

- [ ] **Step 1: 写失败测试**

```python
# tests/test_cross_source_dedup.py
from pathlib import Path
import csv
from acq.normalize_corpus import merge_corpus

def test_merge_dedup(tmp_path):
    b1 = tmp_path / "cnki_b1"; b1.mkdir()
    with open(b1 / "metadata.csv", "w", encoding="utf-8-sig", newline="") as f:
        csv.writer(f).writerows([["title", "doi"], ["中文标题", "10.1/x"]])
    b2 = tmp_path / "intl_b2"; b2.mkdir()
    with open(b2 / "metadata.csv", "w", encoding="utf-8-sig", newline="") as f:
        csv.writer(f).writerows([["title", "doi", "source"], ["English Title", "10.1/X", "oa"]])
    out = merge_corpus(tmp_path)
    rows = list(csv.DictReader(open(out, encoding="utf-8-sig")))
    assert len(rows) == 1   # 10.1/x == 10.1/X 跨源去重
```

- [ ] **Step 2: 跑测试确认失败**
- [ ] **Step 3: 实现 `acq/normalize_corpus.py`**（扫 `corpus/*/metadata.csv`，统一列 `FIELDS`，`dedup_by_doi` 合并，写 `corpus/merged_metadata.csv`）
- [ ] **Step 4: 跑测试确认通过**
- [ ] **Step 5: 提交** — `git commit -m "feat(intl): 跨源(中英)DOI去重 + corpus 归一喂0131"`

---

## Self-Review

- **Spec 覆盖**：Hybrid 三条线（OA / Sci-Hub / CARSI 订阅）+ RateGuard + 发现层 + 统一 schema + 跨源去重 + MCP 集成，全部有对应 Task。✅
- **占位符扫描**：核心算法（PDF 校验/DOI 规范化/直连/guard/OpenAlex/OA/Sci-Hub/编排/去重）均给真实代码；浏览器选择器/`browser.launch_browser` 返回签名/订阅库 `pdf_pattern` 三处明确标注「实施时按真实页面/真实签名核对」（因依赖运行时页面，无法在计划期写死，已给出探测方法而非占位）。✅
- **类型一致性**：`download()` 返回 `(path, source)` 对齐 CNKI `download_paper` 契约；`RateGuard` 方法名贯穿一致；统一 `FIELDS` 在 Task 13/15 一致。✅
- **风险红线**：①直连铁律（Global Constraint 1）贯穿所有网络任务；②订阅库账号风险靠 RateGuard 严配（并发1/10-30s/每日小配额/熔断）；③Sci-Hub 灰色，OA 优先、可开关。✅

## 已知待运行期确认（非占位，是必须实测项）

1. `browser.launch_browser()` 的真实返回签名（Task 11/12 依赖）。
2. Sci-Hub 活镜像会变——`DEFAULT_MIRRORS` / `BBAN_CDN` 需定期实测刷新（Task 7 已留多镜像 + CDN 双路）。
3. 订阅库真实 PDF 选择器/`pdfft` 拼链（Task 12 Step 5 手动验收时确定 EBSCO/ProQuest 的 response 监听细节）。
4. CARSI 登录时国内 IdP 的 Clash DIRECT 规则（Global Constraint 1 例外项；Task 11 用自带 chromium 规避）。

## Execution Handoff

计划已存 `docs/superpowers/plans/2026-06-29-国际论文adapter.md`。两种执行方式：

1. **Subagent-Driven（推荐）** — 每个 Task 派发独立 subagent，任务间我审查，迭代快。
2. **Inline Execution** — 本会话内逐 Task 执行，带检查点。

选哪种？
